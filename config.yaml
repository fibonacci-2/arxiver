llm:
  model: "gpt-4o-mini"
  temperature: 0
  
embeddings:
  model: "text-embedding-3-small"

indexer:
  type: "vector"
  chunk_size: 1000
  chunk_overlap: 200
  top_k: 10

search:
  max_results: 10
  top_papers: 5

output:
  format: "latex"
